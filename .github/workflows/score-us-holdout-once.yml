name: Score U.S. sealed holdout once
on:
  push:
    branches: [resolver-engine-v0]
    paths:
      - "scripts/score-us-holdout-once.mjs"
      - ".github/workflows/score-us-holdout-once.yml"
jobs:
  score:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: {node-version: 24}
      - run: npm install
      - run: node scripts/score-us-holdout-once.mjs
      - uses: actions/upload-artifact@v4
        with:
          name: us-holdout-100-open-once-score
          path: artifacts/us-holdout-100-open-once-score.json
